<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">
    <persistence-unit name="default" transaction-type="RESOURCE_LOCAL">
        <class>com.example.commentary</class> <!-- Asegúrate de usar el nombre correcto de la clase -->
        <properties>
            <!-- Configuración para PostgreSQL -->
            <property name="javax.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property name="javax.persistence.jdbc.url" value="jdbc:postgreSQL12://localhost:5433/Commentary"/>
            <property name="javax.persistence.jdbc.user" value="postgres"/>
            <property name="javax.persistence.jdbc.password" value="virginia"/>
            <!-- Propiedades adicionales según sea necesario -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>
            <!-- Si estás utilizando Hibernate -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <!-- Esto creará automáticamente las tablas según tus entidades -->
        </properties>
    </persistence-unit>
</persistence>

